[project]
name = "bag-parser"
version = "0.1.0"
description = "Turns Dutch addresses database (BAG or Basisregistratie Adressen en Gebouwen) into a DataLake"
readme = "readme.MD"
requires-python = ">=3.14"
dependencies = [
    "duckdb>=1.5.5",
    "psutil>=7.2.2",
]
